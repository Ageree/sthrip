# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6
jinja2>=3.1.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
alembic>=1.12.0

# Cache & Queue
redis>=5.0.0

# HTTP Client
requests>=2.33.0
aiohttp>=3.9.0
httpx[socks]>=0.27.0
# Tor SOCKS5 outbound for `.onion` webhook delivery (Sprint 6).
# Used only when STHRIP_ONION_ENABLED=true and target endswith ".onion".
aiohttp-socks>=0.8.4
# requests SOCKS5 support for SDK Client(use_tor=True).
PySocks>=1.7.1

# Security
cryptography>=41.0.0
pycryptodome>=3.19.0

# Monitoring
psutil>=5.9.6
prometheus-client>=0.19.0

# Error Tracking
sentry-sdk[fastapi]>=2.0.0
logtail-python>=0.3.0

# Utils
python-dotenv>=1.2.2
pyyaml>=6.0.1
tenacity>=8.2.0

# CLI
typer>=0.9.0,<1.0

# MCP Server
mcp>=1.0.0

# E2E Encrypted Messaging
PyNaCl>=1.6.0

# Testing
fakeredis>=2.0.0
